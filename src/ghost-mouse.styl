@import "../node_modules/nib"

.ghost-mouse
  size = 25px

  background: linear-gradient(top, rgba(black, 0.33), rgba(black, 0.67))
  border: 2px solid white
  box-sizing: border-box
  border-radius: 50%
  box-shadow: 0 5px 3px rgba(black, 0.33)
  height: size
  left: 0
  margin: (size / -2) 0 0 (size / -2)
  opacity: 0
  pointer-events: none
  position: absolute
  top: 0
  transform: scale(4)
  transition:
    box-shadow 0.125s ease-in-out,
    opacity 0.25s ease-in-out 0.25s,
    margin 0.125s ease-in-out,
    transform 0.75s ease-in-out
  width: size

  &.inverted
    background: linear-gradient(top, rgba(white, 0.33), rgba(white, 0.67))
    border-color: black

  &.active
    opacity: 1
    transform: scale(1)

  &.down
    background: linear-gradient(top, rgba(red, 0.5), rgba(red, 0.75))
    box-shadow: 0 1px 2px rgba(black, 0.67)
    margin-top: (size / -2) + 2
